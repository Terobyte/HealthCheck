# HealthCheck 🩺

Система мониторинга здоровья ПК с AI-анализом и уведомлениями в Telegram.

## Функциональности

- 📊 Мониторинг системных ресурсов (CPU, RAM, диск, сеть)
- 🤖 AI-анализ состояния системы с использованием Google Gemini
- 📱 Уведомления в Telegram при проблемах
- 💾 Сохранение отчетов офлайн, если нет интернета

## Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd HealthCheck
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

Создайте файл `.env` на основе шаблона `.env.example`:

```bash
cp .env.example .env
```

### 4. Добавьте ваши API ключи в `.env`

Отредактируйте файл `.env` и добавьте ваши API ключи:

```env
# Google Gemini AI API Keys
GEMINI_API_KEY=ваш_gemini_api_key_здесь
GEMINI_API_KEY_FALLBACK=ваш_gemini_fallback_key_здесь

# Telegram Bot Configuration
TELEGRAM_TOKEN=ваш_telegram_bot_token_здесь
TELEGRAM_CHAT_ID=ваш_telegram_chat_id_здесь
```

#### Как получить API ключи:

- **GEMINI_API_KEY**: Получите на [Google AI Studio](https://makersuite.google.com/app/apikey)
- **TELEGRAM_TOKEN**: Получите у [@BotFather](https://t.me/botfather)
- **TELEGRAM_CHAT_ID**: Ваш ID в Telegram (узнать можно у бота @userinfobot)

### ⚠️ Важно по безопасности

- **НИКОГДА** не коммитить `.env` файл в систему контроля версий
- Файл `.env` уже добавлен в `.gitignore`
- Используйте `.env.example` как шаблон для создания `.env`
- Не делитесь своими API ключами

## Использование

### Запуск через GUI

```bash
python main_gui.py
```

### Тестирование AI (asa.py)

```bash
python asa.py
```

### Тестирование мониторинга и отправки (network_sender.py)

```bash
python network_sender.py
```

## Структура проекта

```
HealthCheck/
├── .env                 # ❌ СЕКРЕТНЫЙ - НЕ в git
├── .env.example         # ✅ БЕЗОПАСНЫЙ - Коммитить в git
├── .gitignore           # ✅ БЕЗОПАСНЫЙ - Коммитить в git
├── main_gui.py          # Главное GUI приложение
├── network_sender.py    # Мониторинг и отправка отчетов
├── monitor_sys.py       # Модуль мониторинга системы
├── asa.py               # Тестовый скрипт для Gemini API
├── requirements.txt     # Зависимости Python
└── README.md            # Эта документация
```

## Зависимости

- `psutil>=5.9.0` - Мониторинг системы
- `requests>=2.31.0` - HTTP запросы для Telegram
- `google-genai>=0.3.0` - Google Gemini AI SDK
- `python-dotenv>=1.0.0` - Управление переменными окружения

## Лицензия

MIT